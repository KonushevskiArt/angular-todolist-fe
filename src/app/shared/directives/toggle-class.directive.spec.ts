import { CgangeDoneClassDirective } from './cgange-done-class.directive';

describe('CgangeDoneClassDirective', () => {
  it('should create an instance', () => {
    const directive = new CgangeDoneClassDirective();
    expect(directive).toBeTruthy();
  });
});
